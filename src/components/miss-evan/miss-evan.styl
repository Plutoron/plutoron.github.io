$album = 'ablum'

.{$album}-cover
  .${album}-top 
    .top-cover
      width 160px
      height 160px

    .song-number
      height 20px
      background linear-gradient(to right,transparent,rgba(0,0,0,.02) 30%,#000)
