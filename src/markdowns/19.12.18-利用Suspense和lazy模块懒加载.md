### 利用Suspense和lazy模块懒加载

```
import { Suspense, lazy } from 'react'

const Home = lazy(() => import('@modules/file/home'))

const Main = () => (
  <Suspense fallback=''>
    <Home />
  </Suspense>
)

export default Main
```