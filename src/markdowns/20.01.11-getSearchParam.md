### getSearchParam
```
export const getSearchParam = search => search
  .replace('?', '')
  .split('&')
  .reduce((pre, v) => {
    const arr = v.split('=')
    pre[arr[0]] = arr[1]

    return pre
  }, {})
```