```javascript
const maxSubArray = (nums: num[]) => {
  let max = 0

  nums.reduce((pre, v) => {
    if (pre > 0) {
      pre = pre + v
    } else {
      pre = v
    }

    max = Math.max(pre, max)

    return pre
  }, 0)
}
```