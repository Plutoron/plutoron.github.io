### node 上传打包目录到目标服务器

```javascript
const client = require('scp2');

client.scp( './pub/loginsight/',{
    host: '10.190.12.88',
    username: 'root',
    password: 'Opsfe2020!',
    path: '/root/ecmc8026/pub/loginsight/'
}, (err) => {
  if (err) {
    console.log("err: " + err);
  } else {
    console.log('上传完成')
  }
});
```