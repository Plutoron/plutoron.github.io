```javascript
function fib(n) {
  let result = []
  for (let index = 0; index < n; index++) {
    if (index < 2) {
      result.push(1)
    } else {
      result.push(result[index - 1] + result[index - 2])
    }
  }

  return result
}

console.log(fib(5))
```